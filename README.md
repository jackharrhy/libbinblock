# Bingen

A **bin**block **gen**erator.

## Run locally

Install the latest Node.js LTS release, then run:

```sh
npm install
npm run build
python3 -m http.server 8888
```

Open `http://localhost:8888/dist/`.
